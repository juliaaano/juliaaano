# Deny all by default - whitelist only what's needed for the container build
# Works with both Docker (.dockerignore) and Podman/Buildah (Containerfile)
*

# Required for Jekyll build
!source
!_config.yml
!Gemfile
!Gemfile.lock
